spring.datasource.url=jdbc:h2:mem:db_product_test;DB_CLOSE_DELAY=-1;MODE=MySQL
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop

# Disable Config Server and Eureka client during tests
spring.cloud.config.enabled=false
eureka.client.enabled=false

# Test JWT secret
jwt.secret=mi_clave_super_secreta_para_jwt_segura_2026_abcdef

# Enable SQL logging for debug
spring.jpa.show-sql=true

app.upload.dir=uploads
app.upload.images=images
